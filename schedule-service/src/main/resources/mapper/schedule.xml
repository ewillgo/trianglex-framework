<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.trianglex.schedule.dao.ScheduleDAO">

    <select id="getSchedules" resultType="org.trianglex.schedule.domain.ScheduleItem">
        select *;
        from schedule
    </select>

    <insert id="addSchedule" keyColumn="id" keyProperty="id">
        insert into schedule ()
        values ()
    </insert>

</mapper>